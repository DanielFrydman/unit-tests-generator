# frozen_string_literal: true

RSpec.describe Unit::Tests::Generator do
  it "has a version number" do
    expect(Unit::Tests::Generator::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
